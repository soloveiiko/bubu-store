import Inputs, { usernameField, numberField, emailField, mixField, passwordField } from './Inputs';
export { Inputs, Inputs as default, usernameField, numberField, emailField, mixField, passwordField };
